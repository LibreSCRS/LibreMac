# SPDX-License-Identifier: LGPL-2.1-or-later
#
# What a tag of this repository publishes as release assets. One glob per
# line, then the one-line description the downloads page renders.
#
# --staged compares this list against the files the release workflow staged,
# in the step before `gh release create`; --published <tag> compares it against
# the release that exists. Changing what the workflow publishes without
# changing this file fails the release before it is created.
LibreMac-*-macos.dmg  universal macOS build (x86_64 + arm64) with the agent inside; ad hoc signed, not notarised
sbom-macos.cdx.json   CycloneDX bill of materials for the disk image
SHA256SUMS            checksums over every other asset in this release
*.sigstore.json       cosign keyless signature bundle, one per asset
